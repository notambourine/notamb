{
  "name": "notamb",
  "version": "0.0.1",
  "description": "Sticky A/B bucketing microlib — copy-paste from notamb.notambourine.com, not npm install",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "lint": "eslint .",
    "test": "npm run build && node --test"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.28.2",
    "eslint": "^10.9.1",
    "globals": "^17.11.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.68.0",
    "wrangler": "^4.127.0"
  }
}
